[pytest]
markers =
    integration: requires running services (not run in unit CI)
testpaths =
    orchestrator/tests
    GPU/yolo/tests
    GPU/shared/tests
addopts = -ra -q --strict-markers
norecursedirs = *venv* node_modules __pycache__ *.egg
